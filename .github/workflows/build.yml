name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    # setup MySQL
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 17
      uses: actions/setup-java@v1
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Cache Maven packages
      uses: actions/cache@v1
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Set up MySQL(via Docker Compose)
      run: |
        docker-compose up -d
        sleep 10
        docker ps -a

    - name: Maven Verify
      run: |
        mvn --batch-mode --update-snapshots verify
        mvn clean package

    # - uses: actions/checkout@v2
    # - run: mkdir -p target/
    # - run: echo hello > target/demo-1.jar

    # - uses: actions/upload-artifact@v1
    #   with:
    #    name: demo-1
    #    path: target/
