version: "3"
services:
  # employee:
  #   image: employee
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8089:8089"
  #   networks:
  #     - employee-mysql
  #   depends_on:
  #     - mysql

  mysql:
    image: mysql:8
    env:
      MYSQL_ROOT_PASSWORD: mysecret
      MYSQL_USER: admin
      MYSQL_PASSWORD: password
      MYSQL_DATABASE: test
    ports:
      - "3306:3306"
    networks:
      - employee-mysql

networks:
  employee-mysql: 